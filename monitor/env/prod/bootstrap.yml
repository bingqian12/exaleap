spring:
  application:
    name: monitor


  rabbitmq:
    host: 192.168.33.12
    port: 5672
    username: jzgadmin
    password: Ual0L1Q#^19

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: prod
eureka:
  instance:
    prefer-ip-address: true #使用IP注册
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
    ##续约更新时间间隔设置5秒，m默认30s
    lease-renewal-interval-in-seconds: 30
    ##续约到期时间10秒，默认是90秒
    lease-expiration-duration-in-seconds: 90
  client:
      service-url:
        defaultZone: http://jzg:jzg123@10.8.5.230:8080/eureka/

logging:
  level:
    root: DEBUG

#spring:
#  security:
#      user:
#        name: jzg
#        password: jzg123

ribbon:
  ReadTimeout: 2000 # 请求处理超时时间
  ConnectTimeout: 2000
turbine: #要监听的微服务列表
  app-config: product,customer
  cluster-name-expression: "'default'"